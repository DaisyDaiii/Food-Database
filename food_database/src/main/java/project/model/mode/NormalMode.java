package project.model.mode;

/**
 *  Normal mode class extends Mode class
 *  The class contains all the normal mode actions
 */
public class NormalMode extends Mode {
    public NormalMode(String name, int number) {
        super(name, number);
    }
}
